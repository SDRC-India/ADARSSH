import { ColumnPipePipe } from './column-pipe.pipe';

describe('ColumnPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ColumnPipePipe();
    expect(pipe).toBeTruthy();
  });
});
