import { FacilityFilterPipe } from './facility-filter.pipe';

describe('FacilityFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FacilityFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
