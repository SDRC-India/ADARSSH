import { TableAreasFilterPipe } from './table-areas-filter.pipe';

describe('TableAreasFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TableAreasFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
