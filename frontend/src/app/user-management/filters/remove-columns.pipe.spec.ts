import { RemoveColumnsPipe } from './remove-columns.pipe';

describe('RemoveColumnsPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveColumnsPipe();
    expect(pipe).toBeTruthy();
  });
});
